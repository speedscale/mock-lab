# A 1-second interval keeps short replay windows measurable; production
# systems normally scrape far less often.
global:
  scrape_interval: 1s
  scrape_timeout: 1s

scrape_configs:
  - job_name: checkout-api
    static_configs:
      - targets: ["host.docker.internal:8080"]
